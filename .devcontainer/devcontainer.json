{
    "name": "pico-gs FPGA Development",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "args": {
            "OSS_CAD_SUITE_VERSION": "20260127",
            "OSS_CAD_SUITE_DATE": "2026-01-27"
        }
    },
    "remoteUser": "vscode",
    "features": {
        "ghcr.io/devcontainers/features/git:1": {}
    },
    "runArgs": [
        "--privileged"
    ],
    "initializeCommand": "node -e \"const fs=require('fs'),path=require('path'),home=process.env.HOME||process.env.USERPROFILE;fs.mkdirSync(path.join(home,'.claude'),{recursive:true});fs.existsSync(path.join(home,'.gitconfig'))||fs.writeFileSync(path.join(home,'.gitconfig'),'')\"",
    "mounts": [
        "source=pico-gs-cargo-cache,target=/opt/cargo/registry,type=volume",
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.claude,target=/home/vscode/.claude,type=bind,consistency=cached",
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.gitconfig,target=/home/vscode/.gitconfig,type=bind,consistency=cached"
    ],
    "containerEnv": {
        "OSS_CAD_SUITE_PATH": "/opt/oss-cad-suite",
        "GIT_EDITOR": "code --wait"
    },
    "remoteEnv": {
        "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}"
    },
    "postCreateCommand": "bash .devcontainer/scripts/post-create.sh",
    "customizations": {
        "vscode": {
            "extensions": [
                "mshr-h.VerilogHDL",
                "eirikpre.systemverilog",
                "eamodio.gitlens",
                "ms-vscode.makefile-tools",
                "EditorConfig.EditorConfig"
            ],
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash",
                "terminal.integrated.profiles.linux": {
                    "bash": {
                        "path": "/bin/bash",
                        "args": [
                            "-l"
                        ]
                    }
                },
                "verilog.linting.linter": "verilator",
                "files.associations": {
                    "*.v": "verilog",
                    "*.sv": "systemverilog",
                    "*.lpf": "text"
                }
            }
        }
    },
    "workspaceFolder": "/workspaces/pico-gs"
}